class Expenses {
  int total;
  String category;
  DateTime dateTime;
  String description;
  List<String> images;

  Expenses(
      this.total, this.category, this.dateTime, this.description, this.images);
}
